# Create a new repository
# Clone url (copy) cd (paste) 
# git status (informacia)
# git add "file" or file, or (git add -A (add all files))
# git commit -m "comment"
# git push
# git pull (download)
print("hello world")
